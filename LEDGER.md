# 实验台账（每轮一行，按时间序；只追加不修改）

- [EXP-013 立项] 2026-09-04 | 教师 | **教师首次闭环 bench（教师成绩单 #1）**：产 008/009 标签的那台机器（RamenMctsTrainer 全阶段搜索：sn64 / radical 1.4 / **use_ucb=false** / RamenSelect 合并动作 / 冠军 rollout 评估器）装进选手壳从 turn 0 打满 525 计划×4 局 v4（seed 61444，CRN 配对）。此前从未被 bench；65071.1（EXP-012 v2）是标签自估口径（单步搜索+事后 rollout 走完+cross-fit），下界代理非实测。判读三档（合同）：≥69000 标签有肉继续蒸 / 66000–69000 学生贴顶训练侧收摊 / <66000 蒸馏理解重看。补丁链 006c→d→e→fix→008→013v3；保真锚 hw-default 65438.2 / hw-champion 65554.2 / nn-0831 66734.8（锚臂 8 rep 逐位可比），破位即作废 | 本仓 exp-013.yml | 进行中 | 教师分 = 蒸馏线全部分母的分母 | experiments/EXP-013-teacher-report-card/plan.md
- [EXP-013 运行事故 ×4（无成本损失，全量从未带伤起跑）] 2026-09-04 | run 33906965488：smoke 红——40 局在 900s 没跑完（**实测单局 67.9s，是按上游 12.3s/局预算的 5.5×**）。run 33910064064：审计发现 **v1 补丁漏了 `with_ucb(false)`**（标签采集四前提之一，防 UCB 把样本分配烘进标签）→ v1 跑的是 UCB 变体 ≠ 标签教师，手动 cancel。run 33912219777：smoke 再红——16 局（2 计划并行）900s 仍未完 → 单局核时确证 ≈271 core·s，8 rep 全量 = 316 core·h/臂 → v4 合同修订（4 rep + 砍副口径 + 20 片×108 局）。run 33922282460（v4）：build/smoke 全绿、三锚臂全绿、**教师分片 16/20 全部把 108 局算完后、在 CSV 聚合回查处越界 panic**（`len is 27 but the index is 243`）——v2 patch #7 把 plan_index 语义改成全局计划号（种子流正确），但漏追了它的全部下游消费点：聚合循环仍拿局部切片（27）索引全局号（243）。**~20 片 × 71min 核时白烧、零数据落盘**，手动 cancel 剩余 job。修复 = patch_exp013.py **v3**（新增 patch #8：聚合回查改走 `all_plans` 全表）。教训入库：**①"同款机器"要对采集器 manifest premises 逐条核（与 010 的 −1516 同根）；②单局成本以核时算不以墙钟算；③改一个变量的语义必须追完它的每一个消费点——patch #7 的种子流效果全对，偏偏死在它自己的显示层。**| 已重点火 v5
- [EXP-013 结论] 2026-09-05 09:37（北京时间）| run 33929608360 全绿（21 job 零红）| **教师闭环分 68407.3（se 66.0，n=2100，pt 口径，sn64/ucb-off/冠军 rollout 评估器）→ 三档判读 = 中档：学生已接近教师，训练侧到此为止。**保真锚三枚全逐位复现（偏差 ±0.0）：hw-default 65438.2 / hw-champion 65554.2 / nn-0831 66734.8。CRN 配对：vs hw-default Δ=+2985.5（t=+45.1，胜 1768/2100）；vs hw-champion Δ=+2868.7（t=+44.2）；vs nn-0831 **Δ=+1712.2（t=+26.5，胜 1479/2100）**——教师真分比标签自估 65071.1 高 3336，比学生纪录 nn-0831 高 1712（≈26σ，不是统计噪声）。解读：①天花板是真的——sn64 教师的整局上限 ≈68.4k，"学生快贴住教师"的直觉被证实，**70000 这个数字在当前教师下不可达**（学生纪录距教师上限仅 667 分≈10σ，理论余量 <1%）；②65071.1 → 68407.3 的 3.3k 差距 = "标签自估"与"整局实测"的口径差，事后任何用 65071 计算的教师上限都要重算成 68407；③自选未达标 0 局——教师在 race_shield 之外也是满编完赛；④单局实测 221s 墙钟（p95 266s，4 核）——第三次确认成本模型。合同后续：**不重采教师、不加 sn 剂量（三档合同未触发第 1 档）；训练侧（EXP-011 系列）收摊；向 70000 的路只剩换更强教师（新口径/新评估器，另立项）或口径侧 PRINCIPLES §6.3。**| 账本同步回主仓待办 | experiments/EXP-013-teacher-report-card/plan.md
